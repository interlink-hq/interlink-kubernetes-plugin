apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bastion.fullname" . }}
  labels:
    {{- include "bastion.labels" . | nindent 4 }}
    interlink/role: {{ include "bastion.name" . }}
type: Opaque
data:
  SSH_PRIVATE_KEY: {{ .Values.gateway.ssh.privateKey }}
